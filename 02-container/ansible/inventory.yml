all:
  children:
    lxc-host:
      hosts:
        container-lxc-host:
          ansible_host: 86.119.30.54
          ansible_ssh_common_args: '-o StrictHostKeyChecking=no'  # Disable strict host key checking 
    manager:
      hosts:
        docker-mgr-01:
          ansible_host: 86.119.31.43
          private_ip: 
          ansible_ssh_common_args: '-o StrictHostKeyChecking=no'  # Disable strict host key checking
    workers:
      hosts:
        docker-wrk-01:
          ansible_host: 86.119.30.66
          private_ip: 
          ansible_ssh_common_args: '-o StrictHostKeyChecking=no'  # Disable strict host key checking
        docker-wrk-02:
          ansible_host: 86.119.31.213
          private_ip: 
          ansible_ssh_common_args: '-o StrictHostKeyChecking=no'  # Disable strict host key checking
